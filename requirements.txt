# Core ML
torch>=2.1.0
# torch-geometric            # unused in v1 (GNN drug encoder is a v2 TODO). Uncomment if needed.
rdkit>=2023.9.1              # NOT rdkit-pypi (deprecated wrapper). Import is `from rdkit import Chem`.
scikit-learn>=1.3
scipy>=1.11
pandas>=2.0
numpy>=1.24,<2.0             # pin <2 for compatibility with shap / older torch wheels
pyarrow>=14.0                # parquet

# MLOps
mlflow>=2.8,<3.0
dvc>=3.0
great-expectations>=0.18,<1.0  # 1.x has a different fluent API
pyyaml>=6.0

# API / UI
fastapi>=0.104
uvicorn[standard]>=0.24
streamlit>=1.28
pydantic>=2.0
python-multipart>=0.0.6

# Explainability
shap>=0.43

# Viz
matplotlib>=3.7
seaborn>=0.13

# Utilities
requests>=2.31
tqdm>=4.66
openpyxl>=3.1                # read Cell_Lines_Details.xlsx
